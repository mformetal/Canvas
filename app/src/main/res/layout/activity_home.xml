<miles.scribble.home.CanvasLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/canvas_layout" >

    <Toolbar
        android:id="@+id/canvas_toolbar"
        android:title="@string/app_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <miles.scribble.home.CanvasSurface
        android:id="@+id/canvas_surface"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <include
        layout="@layout/canvas_circle_menu"
        android:id="@+id/canvas_fab_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        app:layout_behavior="miles.scribble.ui.widget.SnackbarBehavior" />

</miles.scribble.home.CanvasLayout>